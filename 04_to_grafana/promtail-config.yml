server:
  http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: patient-generator
    static_configs:
      - targets: [localhost]
        labels:
          app: patient-generator
          __path__: /logs/*.log*
    pipeline_stages:
      - json:
          expressions:
            level: level
            target: target
      - labels:
          level:
          target:
